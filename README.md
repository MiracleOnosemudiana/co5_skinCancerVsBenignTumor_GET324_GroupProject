# Skin Cancer vs Benign Tumor Classification

A Streamlit application that classifies dermoscopic skin lesion images as either:

- Benign Tumor
- Skin Cancer

## Model
Custom Convolutional Neural Network (CNN)

## Requirements

Install dependencies:

pip install -r requirements.txt

Run the application:

streamlit run app.py
